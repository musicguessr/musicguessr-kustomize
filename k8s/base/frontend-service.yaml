apiVersion: v1
kind: Service
metadata:
  name: musicguessr-frontend
  namespace: musicguessr
  labels:
    app: musicguessr-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: musicguessr-frontend
